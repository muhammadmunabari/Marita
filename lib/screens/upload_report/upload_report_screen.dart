// Placeholder — will be replaced with full implementation from UI reference.
import 'package:flutter/material.dart';

class UploadReportScreen extends StatelessWidget {
  const UploadReportScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Upload Report — awaiting UI reference')),
    );
  }
}
